package com.example.fx.demo.backend.position.repository;

import com.example.fx.demo.backend.position.domain.SwapRealization;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SwapRealizationRepository extends JpaRepository<SwapRealization, Long> {
}
