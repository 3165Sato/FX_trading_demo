# CODEX.md — 実装ルール

Codex は本ファイルのルールに従って実装する。各機能の指示書(`docs/design/`)は
本ファイルと `DESIGN.md` を**前提**とし、共通ルールを再掲しない。

## 参照規約
- フロントエンド変更時は **`DESIGN.md` を必ず参照**。色・余白・角丸・情報密度・コンポーネント方針は `DESIGN.md` を正とする。
- 機能の詳細仕様は `docs/design/` の各指示書を正とする。
- 本ファイルと指示書が矛盾する場合は、**本ファイル(共通ルール)を優先**し、相違は報告する。

## 全体方針
- DemoFX は学習用の架空FXデモ。実在サービス・実資金・実取引は扱わない。
- 既存の Entity / Repository / Service / Controller / 画面を**壊さない**。
- **小さいステップで進める**。一度に大きな改修をしない。各ステップ完了ごとに変更ファイルと差分概要を報告する。
- 既存エンティティ(FxOrder/Trade/Position/Account/MarginRule/TriggerOrder 等)は**再定義しない**。
  着手前に現状を調べて報告し、足りない項目だけ**最小追加**で整合させる。

## 数値・計算
- 価格・数量・金額は **`BigDecimal`**。価格を `double`/`float` で保持しない(乱数・途中計算の double は可)。
- 丸めは **HALF_UP**。価格は priceScale、数量は quantityScale で丸める。
- 約定価格は **BUY=Ask / SELL=Bid**。建玉のマークは決済方向(LONG=Bid / SHORT=Ask)。

## API / バックエンド
- API は Entity を直接返さず **DTO** で返す。
- 開発用 Security 設定(`/api/**` permitAll 等)に従う。**本番用ではない**。
- 周期的な監視(異常検知・ロスカット・予約注文の発動など)は、**シミュレーターの外で独立スケジュール**で行う。simulator のレート生成ロジックは変更しない。
- 口座のトレード生成(発注・強制決済・トリガー発動)は**口座ロックで直列化**し、競合させない。冪等に作る。
- 設定値はコードに直書きせず **`application.yml`** に出す。既定で既存挙動を再現できるようにする。

## フロントエンド
- API URL を localhost 固定にしない。**`NEXT_PUBLIC_API_BASE_URL`** を使う。
- データ領域は **loading / empty / error** を実装し、失敗で固まらない。
- 既存のポーリング方針に合わせる(過剰な短間隔にしない)。
- UI は `DESIGN.md` のトークン・方針に従う(数値は等幅+tabular-nums、色の意味を厳守)。

## コンテンツ
- ニュース等の文言は**架空・汎用**のみ。実在の組織・人物・出来事を出さない。

## リポジトリ運用
- `logs/` 配下および `*.log` は Git 管理しない。
- Claude 生成のワイヤー/デザインコードは**参照に留め**、既存規約に合わせて実装する(丸ごと貼らない)。

## 指示書テンプレート(`docs/design/` 用)
各機能指示書は以下の構成。冒頭で本ファイルと `DESIGN.md` を参照し、固有部分のみ書く。
1. 準拠(本書 CODEX.md / DESIGN.md を参照する旨)
2. ゴール / 位置づけ
3. スコープ(in / out)
4. 設計概要
5. バックエンド仕様
6. フロントエンド仕様
7. この機能固有の制約
8. 実装ステップ
9. テスト(受け入れ条件)
10. 動作確認手順
